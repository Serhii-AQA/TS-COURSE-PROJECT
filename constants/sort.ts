export const enum SortTypeEnum {
    ASC_NAME = 'asc',
    DESC_NAME = 'desc',
}

export const enum SortValueEnum {
    ASC_NAME = 'name,asc',
    DESC_NANE = 'name,desc',
    ASC_PRICE = 'price,asc',
    DESC_PRICE = 'price,desc',
}
